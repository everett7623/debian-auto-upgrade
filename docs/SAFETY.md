# Safety Boundaries

本项目按以下边界处理高风险系统操作：

- APT/dpkg 锁只能等待释放，不直接删除。
- 第三方源先备份再禁用，恢复由管理员逐项确认。
- 常规升级只刷新 GRUB 配置，不写 MBR。
- 网络配置只备份和检查，不自动改名或重启。
- 升级完成后不自动 `autoremove`。
- 所有升级必须是相邻 Debian 大版本。
- 测试版升级必须显式启用并二次确认。

`--force` 只跳过交互确认，不代表操作无风险，也不应替代快照和控制台访问。
