# Security Policy

## 支持范围

安全修复优先覆盖根目录的 `distro_upgrade.sh`。`scripts/` 下的历史分版本脚本不视为受支持的生产入口。

## 报告问题

请不要在公开 Issue 中发布服务器地址、密钥、完整软件源凭据或业务数据。安全问题可通过仓库维护者公开联系方式私下报告，并包含：

- 受影响版本或提交；
- 可复现步骤；
- 预期与实际行为；
- 可能影响；
- 已隐藏敏感信息的日志。

## 执行远程脚本

建议固定到发布版本或提交，并在执行前核对内容。不要将未经验证的 `curl | bash` 用于生产系统。
