# Legacy Scripts

此目录保留 Debian 8-13 的早期分版本脚本和旧主控脚本，用于历史追踪与迁移参考。

这些脚本可能包含已归档镜像、过时版本状态、直接删除锁文件或自动修改系统服务等旧行为，不作为当前生产入口。请优先使用仓库根目录的 `debian_upgrade.sh`。

如需从 Debian 8-10 迁移，请先阅读每个中间版本的 Debian Release Notes，并在可回滚的虚拟机副本中制定专用方案。
